# Changelog

本專案所有重大改動與功能演進均記錄於此文件。

---

## [2026-08-22] — 盤前情報準備台 P0~P2 資料可信度與體驗全面升級

### 🔴 P0 · 資料正確性與時效對齊
- **美股與跨海連動重點標的即時數據補齊 (`feat`/`fix`)**：
  - `fetch_indices.py` 擴充抓取輝達 (NVDA)、蘋果 (AAPL)、台積電現貨 (2330.TW)、聯電 ADR (UMC)、日月光 ADR (ASX)。
  - 明確區分「抓取失敗 ⚠ 缺漏」與「無資料 —」兩種狀態；缺漏時以琥珀黃色醒目標記。
  - 頂部「源狀態」燈號與抓取缺漏動態連動，發現缺漏時轉為黃燈並可點擊彈出「資料源狀態與缺漏檢查」視窗。
- **開盤倒數排除 2026 台股休市行事曆 & 人話格式 (`fix`/`ux`)**：
  - 內建 2026 年台灣證券交易所休市日表（含國定假日與補假）。
  - 自動跨越週末與連假，精準指向次一交易日 09:00:00。
  - 倒數時間改為人話格式（>24h 顯示「X 天 X 時 X 分」、1h~24h 顯示「X 時 X 分」、<1h 顯示「MM:SS」）。
- **「資料日期」與「產生時間」對齊邏輯確認 (`fix`)**：
  - 驗證週五晚間與週末產出時，目標自動對齊週一（08/24），狀態標籤顯示綠色「已對齊次日」。

---

### 🟠 P1 · 可用性、排版與無障礙 (A11y)
- **「差 1 次就處置」高密度排版優化 (`refactor`)**：
  - 多條件（超過 2 條）預設收合為「符合（+N 條件）▾」，點擊即時展開，大幅減輕橫向與縱向擁擠。
  - 圓點進度（●●○）補齊 `title` 與 `aria-label` 描述（如「近 3 日累積觸發 2 次」）。
  - 處置撮合進度標籤改為 `第1次·10分`、`第2次·2分` 並套用 `white-space: nowrap`，徹底根除異常斷行。
- **行動裝置與小型螢幕適配 (375px) (`style`)**：
  - 所有資料表格外層均套用 `<div class="table-scroll">`，手機上支援流暢平移。
  - 頂部導覽列在手機端支援平滑橫向滾動（隱藏滾動條），重整按鈕文案精簡為「重整」，避免按鈕截斷與爆版。
- **無障礙標記完善 (`a11y`)**：
  - 所有表格新增 `<caption class="sr-only">` 語音導讀描述。
  - 所有 `<th>` 補齊 `scope="col"` 語意標籤。
  - 所有外部連結 (`target="_blank"`) 補齊 `rel="noopener noreferrer"` 安全屬性。

---

### 🟡 P2 · 架構、快取與可維護性
- **資料與程式分離 (`arch`)**：
  - 於建置時同步輸出獨立的 `docs/data_latest.json` 完整資料快照。
  - HTML 主檔內嵌入明確標記 `<!-- ===== DATA SNAPSHOT START (auto-generated) ===== -->`。
- **Service Worker 快取策略精細化 (`perf`)**：
  - 升級 `sw.js` 至 `premarket-v3`：App Shell 採用 `stale-while-revalidate`，每日動態 JSON 資料採用 `network-first` 並帶快取備援。
  - `manifest.json` 補齊 PWA 獨立視窗設定與主題色同步。
- **健壯性與匯出功能強化 (`feat`)**：
  - 「📋 匯出重點」Markdown 頂部自動標註「產生時間」與「資料日期（已對齊次日）」。
  - 監聽 `visibilitychange` 事件，分頁背景暫停計時並於回到前景時即時校準，消除倒數時間漂移。
