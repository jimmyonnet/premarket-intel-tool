# Changelog

本專案所有重大改動與功能演進均記錄於此文件。

---

## [2026-08-22] — 盤前情報準備台 P0~P3 全面優化與健壯性修復

### 🔴 P0 · 功能性 Bug 修復
- **修復「即時自結」面板載入逾時卡死 (`fix(P0-1)`)**：
  - 於 `build_embeds.py` 移除 `#app { min-height: 70vh; }` 強制高度，加入 3.5 秒逾時保護，若非同步載入逾時自動收合並顯示「⚠️ 資料連線逾時（已沿用 build-time 快照）」。
  - 主頁面加入 5 秒全域安全逾時機制，避免留下大面積空白。
- **美股與跨海連動重點標的即時數據健全 (`fix(P0-2)`)**：
  - 成功抓取並呈現輝達 (NVDA 214.72)、蘋果 (AAPL 309.35)、台積電現貨 (2330.TW 2,410.00)、聯電 ADR (UMC 18.34)、日月光 ADR (ASX 36.51)。
  - 建立清楚降級機制：抓取失敗顯示「資料缺」並標註上次成功更新時間，盤後現貨標註「(昨收)」。
- **消除全站 `NaN` / `null` / `undefined` 輸出 (`fix(P0-3)`)**：
  - 建立全站共用格式化函式 `fmt_num` 與 `fmt_pct`（支援 Jinja 模板與客戶端 JS），無效數值一律安全輸出「—」，徹底消除 DOM 裸露 `NaN` / `null`。

---

### 🟠 P1 · 資料一致性與正確性
- **統一警示檔數計數口徑 (`fix(P1-4)`)**：
  - `<title>` 格式改為明確口徑：`盤前 · MM/DD · 近處置 X · 出關 Y`。
  - 全站導覽徽章與表格只由單一變數（近處置 + 出關）計算。
- **台指期夜盤標明漲跌基準 (`feat(P1-5)`)**：
  - 夜盤卡片明確標明「基準：vs 期貨日盤收盤 (45,138)」，避免與現貨昨收混淆。
- **財經行事曆未公布狀態與降級 (`feat(P1-6)`)**：
  - 尚未公布之事件於「實際」欄明確標註「未公布」，預期值照常呈現；抓取失敗時標註「缺」。
- **事件倒數 Tooltip 台北時間換算 (`feat(P1-7)`)**：
  - 倒數徽章加上 `title="台北時間：MM/DD HH:mm"` 提示。

---

### 🟡 P2 · 使用體驗優化（自用效率向）
- **「我在看」空狀態引導與清單標記連動 (`feat(P2-8/9)`)**：
  - 自選股為空時顯示引導文案。
  - 自選股出現在任何清單（差1次處置、今日出關、無族群標的）時，該列自動標上醒目金黃色 `★`。
  - 自選股中有標的接近處置時，頁首自動彈出 `⚠️ 您在看的自選股中有 N 檔接近處置標準` 警示橫幅。
- **「族群聚焦標的」候選比對數顯示 (`feat(P2-8)`)**：
  - 當日無族群連動時顯示「今日無族群連動標的（已比對 N 檔候選股）」。
- **行動版表格橫向捲動與導覽列自適應 (`style(P2-10)`)**：
  - 所有表格包覆 `.table-scroll`，導覽列在 375px~1200px 寬度下彈性收納，按鈕不換行截斷。
- **新聞 Top 5 時間統一與舊聞標記 (`feat(P2-11)`)**：
  - 統一顯示「MM/DD HH:mm（台北）」，超過 24 小時之新聞標註「舊」標籤。

---

### 🟢 P3 · 維運與工程衛生
- **Build 資料源健康檢查與異常橫幅 (`feat(P3-12)`)**：
  - 來源異常時頁面頂部顯示「⚠️ 部分資料來源更新異常，已自動啟用快照備援」警示橫幅。
- **無障礙標記完善 (`a11y(P3-13)`)**：
  - 表格補齊 `<caption class="sr-only">` 與 `<th scope="col">`，互動元件補齊 `aria-label`。
