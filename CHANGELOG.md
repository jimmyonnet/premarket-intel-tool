# Changelog

本專案所有重大改動與功能演進均記錄於此文件。

---

## [2026-08-22] — 盤前情報準備台全方位優化與修正

### 🐛 P0 · Bug 修正與核心邏輯修復
- **日期核對邏輯升級 (`fix`)**：
  - 導入 2026 年台灣證券交易所休市行事曆（含國定假日與週末跳過邏輯）。
  - 若資料日期對齊產生時間所屬交易日之「次一交易日」（例如週五/週六產出週一 08/24 盤前），狀態徽章顯示綠色 `is-ok`「已對齊次日」；若為當日顯示藍色「當日盤後」；非預期跨日才顯示紅色「日期待確認」。
- **`⏸` 處置符號間距與高亮 (`fix`)**：
  - 拆分股票代號中之全形 `⏸` 符號為獨立 `<span class="pause-icon" title="處置中">⏸</span>`，右側補齊 `4px` 間隔。
  - 處置中股票所在表格列 (`<tr>`) 自動套用 `.row-paused`，帶淡琥珀色背景高亮。
- **深色模式語意色系完整適配 (`style`)**：
  - 全面定義 CSS 語意顏色對（`--danger-bg/fg/border`, `--amber-bg/fg/border`, `--blue-bg/fg/border`, `--green-bg/fg/border`）。
  - `:root[data-theme="dark"]` 中將 `--rise` 調至 `#FB7185`、`--fall` 調至 `#4ADE80`、`--text-muted` 調至 `#B4BCC8`，徹底解決淺底白字問題並達 AA 級對比度。
- **`theme-color` Meta 標籤連動 (`style`)**：
  - 切換深/淺色主題時動態同步 `meta[name="theme-color"]`（`#0B1220` vs `#F8FAFC`）。
- **Service Worker 離線 Fallback 與 Stale-While-Revalidate (`perf`)**：
  - 升級 `sw.js` 快取版本至 `premarket-v2`，加入完整的 Stale-While-Revalidate 機制，離線時無縫回退至本機快取頁面。

---

### 🧭 P1 · 資訊架構 & UX 強化
- **全域「⭐ 只看自選」過濾 (`feat`)**：
  - 頂部導覽列新增 `⭐ 只看自選` 按鈕，一鍵隱藏全頁非自選股之表格列與卡片，並動態重算警示 Badge 數值。
- **Scroll-Spy 滾動導覽高亮 (`feat`)**：
  - 使用 `IntersectionObserver` 監聽章節區塊，依目前可視位置高亮導覽列對應項目。
- **Back-to-top 懸浮回到頂部按鈕 (`feat`)**：
  - 滾動超過 600px 自動浮現，平滑滾動至頁首。
- **Watchlist（我在看）全面升級 (`feat`)**：
  - 自選股 Chip 顯示動態狀態圖示（🚨 差1次處置 / 🟡 今日出關 / 🔵 未反映公告 / ⚪ 一般）。
  - 點擊 Chip 可平滑滾動跨區塊定位至對應股票 row，並觸發 2 秒輪廓閃爍高亮。
  - 輸入框支援 Enter 鍵快捷儲存，並提供 1.5 秒自動淡出 Toast 提示。
- **財務公告 iframe 預設展開優化 (`perf`)**：
  - 「即時自結」維持預設展開，「即時季報」與「即時營收」改為預設收合並帶筆數 Badge，顯著減少初次載入負載與版面破碎。
- **印刷與截圖模式 (`feat`)**：
  - 支援 `@media print` 純淨報表排版。
  - 導覽列新增「📸 截圖」按鈕，展開核心卡片並隱藏導覽列與頁尾，方便直接截圖分享。

---

### 📊 P1 · 資料視覺化與排版細化
- **處置條件兩行式階層佈局 (`refactor`)**：
  - 第一行呈現已符合款次（綠色圓形 Badge），第二行以獨立 Pill 標籤呈現接近條件與量能但書，未觸及款次以淡灰字收納。
- **手機端 `badge-dots` 轉為 Mini 進度條 (`style`)**：
  - 桌機顯示完整圓點陣列，手機端轉為 `mini-bar` 橫向進度條與數字，卡片視圖下緊湊排版。
- **前端原生表格欄位點擊排序 (`feat`)**：
  - 點擊表格欄位標題 (`<th>`) 即時切換升冪 / 降冪排序，支援數值與中文名稱排序。
- **美股延遲時間動態 Pill (`style`)**：
  - 美股指數卡片以 `X.Xh ago` 色彩標籤展示時效（<24h 灰、24-36h 琥珀、>36h 紅），上方統一標明資料延遲資訊。

---

### ⌨️ P2 · 鍵盤效率與可及性
- **全域鍵盤快捷鍵 (`feat`)**：
  - `1` ~ `6`：快速跳轉各章節
  - `/`：聚焦自選股輸入框
  - `d`：切換深色模式
  - `w`：切換只看自選模式
  - `t`：平滑滾動回頂部
  - `?`：彈出鍵盤快捷鍵說明視窗
  - `Esc`：關閉彈窗與截圖模式
- **URL Hash 狀態同步 (`feat`)**：
  - 主題、自選過濾狀態與錨點自動同步至 URL Hash（例如 `#summary?theme=dark&watch=1`），重新整理即可精準還原。
- **無障礙 (A11y) 完善 (`a11y`)**：
  - 所有按鈕補齊 `type="button"` 與 `aria-label`。
  - 區塊捲動邊界 `scroll-margin-top: 76px` 避免導覽列遮擋錨點。
  - 表格補上 `<caption class="sr-only">`。

---

### 🧹 P3 · 效能優化與清潔
- **頁面可見度節流 (`perf`)**：
  - 監聽 `visibilitychange` 事件，分頁在背景時自動暫停計時器與開盤倒數，回到前景即時恢復。
- **動態 `<title>` 摘要化 (`refactor`)**：
  - 頁面標題改為 `盤前 · MM/DD · 警示 N · 今日 X 檔近處置`，便於釘選分頁即時掌握重點。
- **清理防快取標籤 (`clean`)**：
  - 移除 `<head>` 中的 `Cache-Control` meta 標籤，改由 Service Worker 快取機制統一掌管。
